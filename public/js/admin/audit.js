// audit.js — Tab Auditoria (preenchido na Task 13)
export async function renderAudit(container) {
  container.innerHTML = '<div style="padding:2rem;text-align:center;color:var(--muted)">Tab Auditoria — em construção</div>';
}
