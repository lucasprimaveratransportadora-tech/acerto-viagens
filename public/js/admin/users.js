// users.js — Tab Usuarios (preenchido na Task 11)
export async function renderUsers(container) {
  container.innerHTML = '<div style="padding:2rem;text-align:center;color:var(--muted)">Tab Usuários — em construção</div>';
}
