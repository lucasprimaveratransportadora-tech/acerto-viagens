// logins.js — Tab Logins (preenchido na Task 14)
export async function renderLogins(container) {
  container.innerHTML = '<div style="padding:2rem;text-align:center;color:var(--muted)">Tab Logins — em construção</div>';
}
